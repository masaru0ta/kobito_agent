# 仕様書: Web UI

## 1. 概要

ユーザーがエージェントを操作するためのWeb UI。
FastAPIでHTMLを配信し、REST API + SSEで通信する。

**この仕様の範囲**: 3ペインレイアウト、全5タブの画面構成、ナビゲーション
**範囲外**: 各タブのバックエンド処理（spec_chat.md, spec_runner.md, spec_config.md, spec_settings.md, spec_trigger.md を参照）

**対応デバイス**: PC（デスクトップブラウザ）
**使用技術**: Python (FastAPI + uvicorn), HTML, CSS, JavaScript (バニラ)

## 2. 補足資料

### 2.1 モックアップ

HTMLモックアップ: `docs/mockup_phase3.html`

### 2.2 参照ドキュメント
- `docs/spec_config.md` — エージェント定義、AgentInfo
- `docs/spec_chat.md` — チャット処理、SSEイベント形式
- `docs/spec_runner.md` — 自律思考、ログ・成果物API
- `docs/spec_settings.md` — 設定編集API
- `docs/spec_trigger.md` — トリガーAPI

### 2.3 データ構造とUIの対応

UIはエージェントのデータ構造に1対1で対応する。

```
エージェント名           ← 左ペイン
  ├─ チャット履歴 → チャット  ← 中ペイン（タブ+リスト） → 右ペイン（詳細）
  ├─ 思考履歴 → 思考
  ├─ ミッション → タスク
  ├─ 成果物一覧 → 成果物
  └─ 設定 (CLAUDE.md, config.yaml)
```

## 3. 画面構成

### 3.1 3ペインレイアウト

```
┌──────────┬──────────────────┬──────────────────┐
│ 左ペイン   │ 中ペイン           │ 右ペイン           │
│ (20%)     │ (40%)             │ (40%)             │
│           │                   │                   │
│ エージェント│ タブ + リスト/内容  │ 選択項目の詳細     │
│ 一覧       │                   │                   │
└──────────┴──────────────────┴──────────────────┘
```

**ペイン比率**: 20 : 40 : 40（中↔右ペイン間にリサイズハンドルあり、ドラッグで比率変更可能）

**右ペインの保持**: 中ペインのタブを切り替えても、右ペインの表示は維持される。チャットを開いたまま、ミッションやログを確認できる。

### 3.2 左ペイン（エージェント選択）

- ロゴ「kobito_agent」
- セクションタイトル「エージェント」
- エージェント一覧（名前 + 説明）
- 選択中のエージェントはハイライト表示（左ボーダー + 背景色）
- ページ読み込み時に `GET /api/agents` で取得
- 最初のエージェントを自動選択

### 3.3 中ペイン（タブ + コンテンツ）

上部にタブバー。タブは以下の5つ:

| タブ | 表示形式 | 内容 |
|------|---------|------|
| チャット | リスト型 | 会話一覧 + 新規会話ボタン |
| 思考 | リスト型 | 思考実行ボタン + 思考履歴一覧 |
| ミッション | 直接表示型 | mission.md + task.md |
| 成果物 | リスト型 | ファイル一覧（ファイル名 + サイズ） |
| 設定 | 直接表示型 | config.yaml編集 + CLAUDE.md編集 |

**リスト型タブ**: リストアイテムをクリックすると、右ペインに詳細が表示される。
**直接表示型タブ**: 中ペインに内容を直接表示する。右ペインは変更しない。

### 3.4 右ペイン（詳細表示）

中ペインのリストで選択した項目の詳細を表示する。

| 元タブ | 右ペインの内容 |
|--------|-------------|
| チャット | チャット画面（メッセージ履歴 + 入力欄） |
| 思考 | 思考ログ詳細（成功/失敗バッジ、プロンプト、応答） |
| 成果物 | Markdownプレビュー |

## 4. タブ別仕様

### 4.1 チャットタブ

#### 中ペイン
- **新規会話ボタン**: 右ペインのチャット画面をクリアし、新しい会話を開始
- **会話一覧**: エージェント選択時に `GET /api/agents/{id}/conversations` で取得
  - 各行: 更新日時 + メッセージ件数 + タイトル（あれば）or 最後のメッセージ（切り詰め）
  - タイトルは要約実行後に表示される（会話データの `title` フィールド）
  - 選択中の会話はハイライト表示
  - 最新の会話を自動選択
- **新規会話の仮アイテム**: メッセージ送信時に会話一覧の先頭に仮アイテム（`conv-in-progress`）を追加。別画面に移動しても戻れるようにする
  - プレビューにユーザーのメッセージを表示
  - `onConversationId` でIDが来たら紐付け
  - 応答完了後に `loadConversations()` で正式な一覧に置き換わる

#### 右ペイン（チャット画面）
- **チャットヘッダー**（会話選択時に表示）
  - 1行目: タイトル（あれば）or 日時 + アクションボタン
  - 2行目: 要約（あれば、全文表示）
  - **アクションボタン**:
    - 「要約する」: `POST /api/agents/{id}/conversations/{conv_id}/summarize` を呼び、タイトル+要約を生成・保存。実行中は「要約中...」表示
    - 「CLI起動」: `POST /api/agents/{id}/launch-cli` でClaude CLIをターミナル起動（session_idがあれば --resume）
    - 「削除」: 確認ダイアログ後に `DELETE /api/agents/{id}/conversations/{conv_id}` で削除
- **メッセージ表示**: ユーザーは右寄せ、エージェントは左寄せ
- 各メッセージに送信者名ラベル（ユーザー: 「あなた」、エージェント: エージェント名）
- 各メッセージにタイムスタンプを表示
- エージェント応答はMarkdown（marked.js）でHTMLにレンダリング
- 新しいメッセージ表示時に自動スクロール
- ストリーミング中はカーソルアニメーション表示
- **入力エリア**: テキストエリア + 送信ボタン
  - Enter で送信、Shift+Enter で改行
  - 送信中は入力を無効化
  - 空メッセージは送信不可

#### 会話データ構造
```json
{
  "conversation_id": "uuid",
  "agent_id": "adam",
  "created_at": "ISO8601",
  "updated_at": "ISO8601",
  "session_id": "claude session id or null",
  "title": "要約で生成されたタイトル or null",
  "summary": "要約で生成された要約文 or null",
  "messages": [
    { "role": "user", "content": "...", "timestamp": "ISO8601", "source": "web" }
  ]
}
```

#### ストリーミング中の画面遷移
- 送信中のチャット状態（ユーザーメッセージ、エージェント名、bubble参照、蓄積テキスト）を `streamingChat` 変数に保持
- 別の会話を選択しても自由に切り替え可能
- `conv-in-progress` をクリックして戻ると、`streamingChat` のデータから再描画し、bubble参照を差し替え。以降の `onChunk` は新しいbubbleに追記される
- エージェント切り替え時に送信中なら確認ダイアログを表示

#### 処理フロー（メッセージ送信）
1. 入力テキストを取得し、入力欄をクリア
2. 新規会話なら会話一覧に仮アイテムを追加
3. `streamingChat` にストリーミング状態を保持
4. ユーザーメッセージを右ペインに即座に追加
5. `POST /api/agents/{id}/chat` をfetchで送信
6. `conversation_id` イベント受信 → 内部状態を更新
7. `chunk` イベント受信 → `streamingChat.bubble` にテキストを追加（30文字チャンク分割）
8. `done` イベント受信 → `streamingChat` クリア、入力を再有効化、中ペインの会話一覧を更新

#### 処理フロー（要約）
1. 「要約する」ボタンをクリック
2. `POST /api/agents/{id}/conversations/{conv_id}/summarize` を呼び出し
3. バックエンドが会話内容をテキスト化し、`Runner.summarize_text()` でLLMにタイトル+要約を生成させる
4. 生成結果を会話データの `title` / `summary` フィールドに保存
5. 右ペインのヘッダーにタイトル+要約を表示
6. 中ペインの会話一覧を更新（タイトルが反映される）

### 4.2 思考タブ

#### 中ペイン
- **「新しく自律思考」ボタン**: 新規セッションで `POST /api/agents/{id}/think` を呼び出し
- **「続きで自律思考」ボタン**: 前回のセッションを継続して `POST /api/agents/{id}/think?resume=true` を呼び出し。キャッシュが効くため高速
- **「プロンプト」ボタン**: 右ペインに思考プロンプト（`think_prompt.md`）の編集UIを表示
  - デフォルトプロンプトが表示され、カスタマイズ可能
  - `GET/PUT /api/agents/{id}/think-prompt` で取得・保存
- **思考実行中**:
  - 両ボタンを「思考中...」に変更し無効化
  - 思考履歴一覧の先頭に「思考中」（パルスアニメーション）のアイテムを追加
  - プレビューに最新のイベント（ツール名やテキスト）をリアルタイム更新
  - アイテムをクリックすると右ペインにストリーミング画面を表示（別画面から戻れる）
  - 完了後にボタンを元に戻し、思考履歴一覧を再読み込み
- **思考履歴一覧**: `GET /api/agents/{id}/logs` で取得
  - 各行: 成功/失敗バッジ + 時刻 + 要約（切り詰め）
  - 選択中のログはハイライト表示

#### 右ペイン（ストリーミング中）
- 「思考中」バッジ（パルスアニメーション）
- プロンプト（▶ クリックで展開/折りたたみ、デフォルト非表示）
- リアルタイムログ: ツール使用イベント（⚙アイコン + 緑色）とテキストイベントが順次追加
- 完了時: バッジが「成功」/「失敗」に変わり、結果セクションに報告テキストを表示

#### 右ペイン（思考詳細 — ログ履歴）
- 成功/失敗バッジ + タイムスタンプ
- プロンプト（▶ クリックで展開/折りたたみ）
- 実行過程: `events` 配列からツール使用・テキストを再現表示（ストリーミング中と同じ見た目）
- 結果: 報告テキスト
- 失敗時: エラーメッセージ

#### ストリーミング中の画面遷移
- ストリーミングイベントは `thinkStreamEvents` 配列に保持される
- 別の画面に行ってもイベントは裏で蓄積され続ける
- 思考履歴の「思考中」アイテムをクリックすると、蓄積されたイベントから画面を再構築
- 思考中にClaude Codeが `task.md` / `mission.md` を変更した場合、ミッションタブの表示を自動更新

### 4.3 ミッションタブ

#### 中ペイン
- **「ミッションについて話す」ボタン**: 右ペインで新規会話を開始し、ミッション/タスクについてエージェントと対話する
  - mission.mdがある場合: 「mission.md と task.md を読んで、現状の課題や次にやるべきことについて話そう」を送信
  - mission.mdがない場合: 「まだ mission.md がないので、一緒にミッションを考えよう。CLAUDE.md を読んで、自分の役割からミッションを提案して」を送信
  - 中ペインはミッション表示のまま維持（タブ切り替えしない）
- **「編集」ボタン**: 表示モードと編集モードを切り替え
- **表示モード**: ミッション (mission.md) + タスク (task.md) をMarkdownレンダリング
- **編集モード**: テキストエリアでMarkdownを直接編集。「保存」「キャンセル」ボタン
  - 保存: `PUT /api/agents/{id}/mission` + `PUT /api/agents/{id}/task` で書き込み
  - キャンセル: 変更を破棄して表示モードに戻る
- mission.md / task.md はエージェント選択時の `GET /api/agents/{id}` レスポンス内の `mission` / `task` フィールドから取得

#### 右ペイン
変更しない（前回の表示を保持）

### 4.4 成果物タブ

#### 中ペイン
- **ファイル一覧**: `GET /api/agents/{id}/outputs` で取得
  - 各行: ファイル名 + ファイルサイズ
  - 選択中のファイルはハイライト表示

#### 右ペイン（成果物プレビュー）
- `GET /api/agents/{id}/outputs/{filename}` で内容を取得
- MarkdownをHTMLにレンダリングして表示

### 4.5 設定タブ

#### 中ペイン（直接表示）
- **基本設定 (config.yaml)**
  - 名前（テキスト入力、必須）
  - モデル（テキスト入力、必須）
  - 説明（テキスト入力、任意）
- **トリガー設定**
  - 「定期トリガーを有効にする」チェックボックス
  - cron式（テキスト入力、5フィールド形式。チェックボックス有効時のみ表示）
  - ヒント: 「5フィールド形式（分 時 日 月 曜日）。例: */10 * * * *（10分ごと）」
  - トリガー状態表示: 実行状態（実行中/待機中）、次回実行時刻、最終実行時刻
  - API: `PUT /api/agents/{id}/trigger-config`, `DELETE /api/agents/{id}/trigger-config`
- **システムプロンプト (CLAUDE.md)**
  - テキストエリア（大、等幅フォント、任意）
  - ヒント: 「Markdown形式で記述できます」
- **ボタン**: 「保存」「リセット」

#### 右ペイン
変更しない（前回の表示を保持）

#### 処理フロー
- 詳細は `docs/spec_settings.md`、`docs/spec_trigger.md` を参照

## 5. 処理フロー（共通）

### ページ読み込み時
1. `GET /api/agents` でエージェント一覧を取得
2. 左ペインにエージェント一覧を表示
3. 最初のエージェントを自動選択（→ エージェント選択時の処理へ）

### エージェント選択時
1. 左ペインの選択状態を更新（ハイライト）
2. チャットタブの会話一覧を `GET /api/agents/{id}/conversations` で取得
3. 会話がある場合、最新の会話を自動選択し右ペインに表示
4. 会話がない場合、右ペインをクリア
5. 中ペインの現在のタブに応じて内容を更新

### タブ切り替え時
1. 中ペインのコンテンツを切り替える
2. **右ペインは変更しない**（現在の表示を保持）

### SSEクライアント実装

```javascript
// EventSourceではなく fetch + ReadableStream で実装する
// （EventSourceはPOSTリクエストに対応していないため）
async function sendMessage(agentId, message, conversationId) {
    const response = await fetch(`/api/agents/${agentId}/chat`, {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({ message, conversation_id: conversationId }),
    });
    const reader = response.body.getReader();
    // SSEパースしてイベントをコールバックに渡す
}
```

## 6. サーバー構成

```python
# server/app.py — エントリーポイント
app = FastAPI()
# APIルーター + 静的ファイル配信
app.mount("/", StaticFiles(directory="server/static", html=True))
```

起動: `start.bat` でuvicornを起動（ポート8300、ホットリロード有効）

### 静的ファイル構成

```
server/static/
  index.html        # SPA（シングルページ）
  css/
    style.css       # スタイル
  js/
    app.js          # メインアプリケーション
    api.js          # API通信ユーティリティ
    chat.js         # チャット表示機能
    settings.js     # 設定タブの表示・保存処理
    think.js        # 思考タブの表示処理
```

## 7. 非機能要件

- ホットリロード: uvicornの `--reload` で、サーバーコード変更時に自動再起動
- Markdownレンダリングにはmarked.js（CDN）を使用
- SPA的に1ページで完結。ルーティングは不要
- モバイル対応は行わない
- 認証・認可は行わない（ローカル利用前提）
- ダークモードのみ

## 8. テスト方針

- Playwright MCPでE2Eテストを行う
- テスト用エージェントを作成し、サーバーを起動した状態でテストする
- LLM APIはテスト時もモックする

## 9. テスト項目

### レイアウト
- [ ] 3ペインが横並びで表示される（左20%、中40%、右40%）
- [ ] 左ペインにエージェント一覧が表示される
- [ ] 中ペインに5つのタブ（チャット/思考/ミッション/成果物/設定）が表示される

### エージェント選択
- [ ] エージェントをクリックするとハイライト表示になる
- [ ] エージェントを切り替えるとチャットタブの会話一覧が更新される
- [ ] 会話履歴がある場合、最新の会話が右ペインに自動表示される
- [ ] チャット送信中にエージェントを切り替えると確認ダイアログが表示される
- [ ] 設定の未保存時にエージェントを切り替えると確認ダイアログが表示される

### タブ切り替え
- [ ] タブをクリックすると中ペインのコンテンツが切り替わる
- [ ] タブを切り替えても右ペインの表示が維持される

### チャットタブ
- [ ] 会話一覧が中ペインに表示される（日時 + メッセージ件数 + タイトルorプレビュー）
- [ ] 会話をクリックすると右ペインにメッセージ履歴が表示される
- [ ] 右ペインのヘッダーにタイトル（あれば）とアクションボタンが表示される
- [ ] 新規会話ボタンで右ペインがクリアされる
- [ ] メッセージを送信するとストリーミングで応答が表示される
- [ ] ユーザーメッセージが右寄せ、エージェント応答が左寄せで表示される
- [ ] エージェント応答のMarkdownがHTMLに変換される
- [ ] Enterキーで送信、Shift+Enterで改行
- [ ] 送信中は入力欄と送信ボタンが無効化される
- [ ] 空のメッセージは送信できない
- [ ] 送信後に中ペインの会話一覧が更新される
- [ ] 新規会話の送信時に会話一覧の先頭に仮アイテムが表示される
- [ ] ストリーミング中に別の会話に切り替えられる
- [ ] 仮アイテムをクリックするとストリーミング中の会話に戻れる
- [ ] エージェント切り替え時に送信中なら確認ダイアログが表示される
- [ ] 「要約する」ボタンでタイトルと要約が生成・表示される
- [ ] 「CLI起動」ボタンでClaude CLIが起動する
- [ ] 「削除」ボタンで確認後に会話が削除される

### 思考タブ
- [ ] 「新しく自律思考」「続きで自律思考」「プロンプト」の3ボタンが表示される
- [ ] 思考履歴一覧が中ペインに表示される（成功/失敗バッジ付き）
- [ ] 思考履歴をクリックすると右ペインに実行過程+結果が表示される
- [ ] ログ詳細にプロンプト（展開/折りたたみ）、実行過程、結果が表示される
- [ ] 「新しく自律思考」で新規セッションが開始される
- [ ] 「続きで自律思考」で前回セッションが継続される
- [ ] 実行中にストリーミングイベント（ツール使用、テキスト）がリアルタイム表示される
- [ ] 実行中に思考履歴に「思考中」アイテムが表示される
- [ ] 「思考中」アイテムをクリックするとストリーミング画面に戻れる
- [ ] 思考中にtask.md/mission.mdが変更されるとミッションタブが自動更新される
- [ ] 思考完了後に思考履歴一覧が更新される
- [ ] 「プロンプト」ボタンで右ペインにプロンプト編集UIが表示される
- [ ] プロンプトを編集・保存できる

### ミッションタブ
- [ ] mission.md の内容が中ペインに表示される
- [ ] task.md の内容が中ペインに表示される（チェックボックス付き）
- [ ] 「ミッションについて話す」ボタンで右ペインに新規会話が開始される
- [ ] mission.mdがない場合、ミッション提案メッセージが送信される
- [ ] 「編集」ボタンでテキストエリアに切り替わる
- [ ] 編集して「保存」するとmission.md/task.mdが更新される
- [ ] 「キャンセル」で変更が破棄される
- [ ] 右ペインの表示が維持される

### 成果物タブ
- [ ] ファイル一覧が中ペインに表示される（ファイル名 + サイズ）
- [ ] ファイルをクリックすると右ペインにMarkdownプレビューが表示される

### 設定タブ
- [ ] 名前・モデル・説明・システムプロンプトのフォームが中ペインに表示される
- [ ] トリガー設定（有効/無効チェックボックス + cron式入力）が表示される
- [ ] トリガー有効時にcron式入力欄が表示される
- [ ] トリガー状態（実行状態・次回実行・最終実行）が表示される
- [ ] 保存ボタンで設定が保存される
- [ ] リセットボタンで変更が元に戻る
- [ ] 右ペインの表示が維持される
